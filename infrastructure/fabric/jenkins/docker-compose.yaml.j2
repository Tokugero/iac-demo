  jenkins:
    image: jenkins/jenkins:lts
    ports:
    - "8080:8080"
    - "50000:50000" # For build agents
    volumes:
    - {{ persistent_path }}/jenkins:/var/jenkins_home
    restart: unless-stopped
